generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_PATH")
}

// PostgreSQL-specific configurations can go here
// For example, custom types, extensions, or indexes

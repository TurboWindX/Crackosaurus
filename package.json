{
  "name": "crackosaurus",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "config": "npm run config:tool -w apps/config",
    "config:cli": "npm run config:cli -w apps/config",
    "check": "turbo check",
    "dev": "turbo dev",
    "format": "turbo format",
    "migrate": "npm run migrate -w apps/db",
    "lint": "turbo lint"
  },
  "devDependencies": {
    "turbo": "latest",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "npm@10.2.4",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "11": "^0.0.1"
  }
}
